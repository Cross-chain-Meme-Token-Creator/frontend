"use client"
import { Autocomplete } from "@nextui-org/react"
import React from "react"

export const SearchBar = () => {
    return (
        <Autocomplete label="" labelPlacement="outside">
            {[]}
        </Autocomplete>
    )
}
